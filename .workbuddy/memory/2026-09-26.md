# 2026-09-26

## 复核 09-24 的问题清单（只读核查，未改业务代码）

结论写入 `docs/project-review-2026-09-24.md` §8。

已修复：
- **P0-1 服务端重构中断**：已拆分完成（server/core、athlete、analysis、data-import、strength、training-plan、access、special、__tests__）；`npm run check` 通过；`npx tsx server/index.ts` 正常启动（/api/teams 200、/api/me 401）。index.ts 从 8218 行收敛为装配层。

未修复（逐项实测）：
- P0-2 `/api/me/training-sessions` 仍不存在，实测 404（`server` 下 `/api/me/` 只有 athlete-profile）。
- P0-3 `WeChat Mini Program/config.js` 仍 development + NETWORK_DEBUG=true。
- P1-4 时区：utils/date.js:9 仍本地时区。
- P1-5 index.js:113 onShow 无节流。
- P1-6 无 __usePrivacyCheck__/requirePrivacyAuthorize，但用了 wx.chooseMedia。
- P1-7 assets 916K，olympic-sports 50 PNG 仍无引用，未分包。
- P1-8 login.js 仍 50 张远程 GIF。
- 文档回写未完成（mini-program-analysis.md 未改动）。

新发现（本次首次跑 api-check 才暴露）：
- `npm run api-check` 崩溃：`scripts/coach-daily-todos-mini-check.mjs:37` 用 vm 加载小程序 index.js，modules 桩缺 `../../utils/page-scope` 与 `../../utils/request-guard` → 解构 undefined。README 的验证命令实际跑不通，重构后接口回归未验证。修法见报告 §8.2。

小程序 44 个 vitest 用例仍全绿。
