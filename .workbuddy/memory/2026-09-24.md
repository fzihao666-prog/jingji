# 2026-09-24

## 项目整体复核 + 小程序专项分析（只读分析，未改业务代码）

产出：`docs/project-review-2026-09-24.md`

关键结论（供后续会话直接复用，无需重查）：

- **dev 工作区服务端重构中断，项目不可运行**：`server/utils.ts:10` 与 `:44` 重复声明 `validatePersonName`，且 `userById`/`numberOrNull` 等未导出；`server/index.ts` 缺 import 导致 `npm run check` 大量 TS2304；`npx tsx server/index.ts` 抛 TransformError。网页端/API/小程序联调全部受阻。
- **小程序训练填报接口断链**：`WeChat Mini Program/services/api.js:33-41` 调用 `/api/me/training-sessions`（GET/POST/PUT/DELETE），服务端全仓库（含 HEAD 版本）无此路由，`pages/training-entry` 必然 404。需核实是否在其他分支。
- 小程序基础质量好：单一 API 门面 `services/api.js`、单一请求层 `utils/request.js`（401 防重入、网络日志脱敏）、`utils/request-guard.js` 过期响应防护、`utils/page-scope.js` 统一筛选；44 个 Vitest 用例全绿（`npx vitest run "WeChat Mini Program"`）。
- 待修风险：`config.js` 仍是 `API_ENVIRONMENT='development'` + `NETWORK_DEBUG=true`（发布阻断）；客户端日期用设备本地时区而服务端按 Asia/Shanghai；Tab 页 `onShow` 全量重拉；`wx.chooseMedia` 未配微信隐私授权；`assets/olympic-sports/*.png` 50 张未被引用且未分包；登录页 50 张远程 GIF。
- `docs/mini-program-analysis.md`（09-23）部分结论已过时（仍写 `_pageRequest` 序号与「筛选样板重复」，现已被 request-guard/page-scope 统一），需回写。
